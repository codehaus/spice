<?xml version="1.0"?>
<project>
    <extend>${basedir}/../../tools/maven/project.xml</extend>
    <name>Converter</name>
    <id>spice-converter</id>
    <gumpRepositoryId>components</gumpRepositoryId>
    <currentVersion>0.9.1</currentVersion>
    <inceptionYear>2000</inceptionYear>

    <shortDescription>Convert objects of one type to another type</shortDescription>

    <description>
        The Converter project is a toolkit to help in converting objects of one
        type to another type. Conversion failure may result in default values or
        exceptions. Conversion also accepts an object "context" parameter which the
        application can pass to the converters so that the conversion process can
        convert in a specific context. For example converting String to File objects
        may take a File "context" object that is the base directory relative to which
        the String points.
    </description>

    <developers>
        <developer>
            <name>Peter Donald</name>
            <id>donaldp</id>
            <email>peter@apache.org</email>
            <roles>
                <role>Developer</role>
            </roles>
        </developer>
    </developers>

    <dependencies>
        <dependency>
            <id>excalibur-i18n</id>
            <version>1.0</version>
        </dependency>
    </dependencies>
</project>